"""FlowMind machine-learning subsystem.

Layout
------
ml/features   feature engineering from raw expenses
ml/models     model wrappers (predict + heuristic fallback)
ml/training   reusable training pipelines
ml/inference  the runtime inference engine + model registry
ml/pipelines  end-to-end expenses -> predictions -> insights flow
"""
